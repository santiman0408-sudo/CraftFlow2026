import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 20,
    backgroundColor: "#FFFFFF",
  },

  content: {
    flex: 1,
  },

  card: {
    marginTop: 16,
  },

  section: {
    marginBottom: 24,
  },

  actions: {
    marginTop: 24,
    gap: 16,
  },
});
